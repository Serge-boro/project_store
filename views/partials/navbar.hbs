<nav class='navbar page'>
  <div class='nav-center'>
    <!-- link -->
    <button class='toggle-nav'>
      <i class='fas fa-bars'></i>
    </button>
    <div>
      <ul class='nav-links'>

        {{#if isHome}}
          <li> <a href='/' class='nav-link active'>Home</a></li>
        {{else}}
          <li> <a href='/' class='nav-link'>Home</a></li>
        {{/if}}

        {{#if isProduct}}
          <li> <a href='/products' class='nav-link active'>Products</a></li>
        {{else}}
          <li> <a href='/products' class='nav-link'>Products</a></li>
        {{/if}}

        {{#if isAbout}}
          <li><a href='/about' class='nav-link active'>About</a></li>
        {{else}}
          <li><a href='/about' class='nav-link'>About</a></li>
        {{/if}}

        {{#if isOrder}}
          <li><a href='/orders' class='nav-link active'>Orders</a></li>
        {{else}}
          <li><a href='/orders' class='nav-link'>Orders</a></li>
        {{/if}}

        {{#if isAuth}}
          <li><a href='/auth/logout' class='nav-link'>Out</a></li>
        {{else}}
          {{#if isLogin}}
            <li><a href='/auth/login' class='nav-link active'>Login</a></li>
          {{else}}
            <li><a href='/auth/login' class='nav-link'>Login</a></li>
          {{/if}}
        {{/if}}

      </ul>
    </div>

    {{!-- {{#if isAuth}}
      <div class='toogle-container'>
        <button class='toggle-cart'>
          <i class='fas fa-shopping-cart'></i>
        </button>
        <!-- amount items store -->
        <span class='cart-item-count'><p>{{totalItems}}</p></span>
      </div>
    {{else}}
    {{/if}} --}}
  </div>
</nav>

<!-- sidebar -->
<div class='sidebar-overlay'>
  <aside class='sidebar'>
    <!-- close button -->
    <button class='sidebar-close'>
      <i class='fas fa-times'></i>
    </button>
    <!-- links -->
    <ul class='sidebar-links'>
      <li>
        <a href='/' class='sidebar-link'>
          <i class='fas fa-home fa-fw'></i>
          home
        </a>
      </li>
      <li>
        <a href='/products' class='sidebar-link'>
          <i class='fas fa-couch fa-fw'></i>
          products
        </a>
      </li>
      <li>
        <a href='/about' class='sidebar-link'>
          <i class='fas fa-book fa-fw'></i>
          about
        </a>
      </li>
      <li>
        <a href='/orders' class='sidebar-link'>
          <i class='fa fa-cart-arrow-down'></i>
          orders list
        </a>
      </li>
      {{#if isAuth}}
        <li><a href='/auth/logout' class='sidebar-link'><i
              class='fa fa-cog fa-spin fa-1x fa-fw'
            ></i>Out</a></li>
      {{else}}
        <li><a href='/auth/login' class='sidebar-link'>
            <i class='fa fa-cog fa-spin fa-1x fa-fw'></i>
            Login</a></li>
      {{/if}}
    </ul>
  </aside>
</div>